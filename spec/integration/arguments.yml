<<: spec_base.yml

processors:
  - filter_methods
  - default_constructor
  - cpp_wrapper
  - crystal_binding
  - crystal_wrapper
  - sanity_check

classes:
  Defaults: Defaults

enums:
  Numeral: Numeral

types: { }
