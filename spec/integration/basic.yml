<<: spec_base.yml

processors:
  - filter_methods
  - default_constructor
  - cpp_wrapper
  - crystal_binding
  - crystal_wrapper
  - sanity_check

classes:
  Adder: AdderWrap
  ImplicitConstructor: ImplicitConstructor

types:
  IgnoreMe: # Ignore by type
    ignore: true
  Adder: # Ignore by method name
    ignore_methods:
      - ignoreByName
